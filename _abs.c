/**
 * _abs - it finds and returns the absolute value of a given number
 * @n: number
 *
 * Return: absolute of a number
*/
int _abs(int d)
{
	if (d < 0)
		return (d * -1);
	else
	{
		return (d);
	}
}
